$$$
mkInteger('int',0,12)
$$$

constants:
    A,B,INFINITE
;
initial:
    bucket(A),bucket(B),bucket(INFINITE),  capacity(A,int5),capacity(B,int3),
    contains(A,int0),contains(B,int0), capacity(INFINITE,int12),
    contains(INFINITE , int12)
;

final:
    contains(A,int4)
;

$$$

--mkEquals()
$$$
